//const { I } = inject();

Given('I have a hoover at coordinates {int} width units and {int} height units', (x, y) => { // eslint-disable-line
    // TODO: steps
});

Given('I have a hoover at coordinates {int} width units and {int} height units', (x, y) => { // eslint-disable-line
    // TODO: steps
});

Given('I have dirt to clean a some coordinates', (patches) => { // eslint-disable-line
    // TODO: steps
});

When('I give cleaning instructions to move {word}', (instructions) => { // eslint-disable-line
    // TODO: steps
});

When('I give cleaning instructions to move {string}', (instructions) => { // eslint-disable-line
    // TODO: steps
});

Then('I should see that total number of clean spots is {int}', (patches) => { // eslint-disable-line
    // TODO: steps
});

Then('I should see a hoover at coordinates {int} width units and {int} height units', (x, y) => { // eslint-disable-line
    // TODO: steps
});